# ─── Tsuki Database Configuration (Example) ─────────────────
# Copy file này thành .env và điền thông tin thực của bạn.
# File này an toàn để commit lên Git.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=TsukiDb
DB_USER=postgres
DB_PASSWORD=your_password
